---
name: "deploy-dry"
description: "Run a dry deployment using the claude-config deployment workflow without restarting production. Use when the user asks for deploy-dry, dry run deploy, deployment validation, or the Codex equivalent of the Claude Code /deploy-dry command."
---

# Deploy Dry

Follow the workflow in `../../commands/deploy-dry.md`.

This skill is the Codex replacement for the Claude Code `/deploy-dry` command. Read the referenced command file before acting, then execute the dry-run deployment semantics exactly as described.
