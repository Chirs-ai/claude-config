---
name: "deploy"
description: "Deploy the current project to a remote server using the claude-config deployment workflow. Use when the user asks for $deploy, deploy, remote deploy, or server restart after pull in Codex."
---

# Deploy

Follow the workflow in `../../commands/deploy.md`.

Codex skill for `$deploy`. Read the referenced command file before acting, then execute its workflow against the current repository. Use `~/.codex/templates/` for Codex-side templates when a template path is needed.
