---
name: "deploy-init"
description: "Initialize claude-config deployment files for a project. Use when the user asks for $deploy-init, deploy-init, deployment setup, create .server.secret, or create run.sh in Codex."
---

# Deploy Init

Follow the workflow in `../../commands/deploy-init.md`.

Codex skill for `$deploy-init`. Read the referenced command file before acting, then execute its workflow against the current repository. Use `~/.codex/templates/` for Codex-side templates when a template path is needed.
