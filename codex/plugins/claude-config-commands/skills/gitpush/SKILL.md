---
name: "gitpush"
description: "Commit and push current Git changes using the claude-config workflow. Use when the user asks for $gitpush, gitpush, commit and push, or one-shot git publish in Codex."
---

# Gitpush

Follow the workflow in `../../commands/gitpush.md`.

Codex skill for `$gitpush`. Read the referenced command file before acting, then inspect status, diffs, staged changes, and recent commit style before staging, committing, and pushing. Do not add `Co-Authored-By`.
