---
name: "gitpush"
description: "Commit and push current Git changes using the claude-config workflow. Use when the user asks for gitpush, commit and push, one-shot git publish, or the Codex equivalent of the Claude Code /gitpush command."
---

# Gitpush

Follow the workflow in `../../commands/gitpush.md`.

This skill is the Codex replacement for the Claude Code `/gitpush` command. Read the referenced command file before acting, then inspect status, diffs, and recent commit style before staging, committing, and pushing. Do not add `Co-Authored-By`.
