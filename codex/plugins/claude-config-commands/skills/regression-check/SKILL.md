---
name: "regression-check"
description: "Run the claude-config regression verification workflow. Use when the user asks for regression-check, regression testing, verify recent changes, smoke test, or the Codex equivalent of the Claude Code /regression-check command."
---

# Regression Check

Follow the workflow in `../../commands/regression-check.md`.

This skill is the Codex replacement for the Claude Code `/regression-check` command. Read the referenced command file before acting, then perform the requested verification and report concrete evidence.
