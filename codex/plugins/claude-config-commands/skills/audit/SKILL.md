---
name: "audit"
description: "Run the claude-config audit workflow. Use when the user asks for audit, code audit, repository inspection, architecture review, risk review, or the Codex equivalent of the Claude Code /audit command."
---

# Audit

Follow the workflow in `../../commands/audit.md`.

This skill is the Codex replacement for the Claude Code `/audit` command. Read the referenced command file before acting, then execute its workflow against the current repository. Keep the same semantics as the original command, but communicate and operate as Codex.
